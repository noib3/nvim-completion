[workspace]

members = [
  "completion-client",
  "completion-core",
  "completion-derive",
  "completion-plugin",
  "completion-sources/*",
  "completion-types",
]

exclude = [ "completion-sources/runtime" ]

[profile.release]
lto = true
codegen-units = 1
