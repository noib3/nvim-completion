mod completion_config;
mod config;
mod sources;
mod ui_config;

use completion_config::CompletionConfig;
pub(crate) use config::Config;
use config::SourcesConfig;
pub(crate) use sources::SOURCE_NAMES;
use ui_config::UiConfig;
